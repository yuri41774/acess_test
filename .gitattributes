# Example: track large binaries with Git LFS if you must version them
# dist/*.exe filter=lfs diff=lfs merge=lfs -text
# dist/** filter=lfs diff=lfs merge=lfs -text
# node_modules/** filter=lfs diff=lfs merge=lfs -text

# Add patterns below if you decide to use Git LFS
